using EnhancedUI;
using EnhancedUI.EnhancedScroller;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CellviewUiCritter : EnhancedScrollerCellView
{
   /* public LevelRowCell[] rowCellViews;

    public void SetData(ref SmallList<LevelSelectItemData> data, int startingIndex)
    {
        for (var i = 0; i < rowCellViews.Length; i++)
        {
            rowCellViews[i].SetData(startingIndex + i < data.Count ? data[startingIndex + i] : null);
        }
    }*/
}
